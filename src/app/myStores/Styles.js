// @flow

const styles = theme => ({
  root: {
    width: '100%',
    marginTop: theme.spacing.unit * 3,
    overflowX: 'auto',
  },
  table: {
    minWidth: 700,
  },
  fabButton: {
    margin: theme.spacing.unit,
  },
});

export default styles;
