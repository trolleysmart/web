// @flow

const styles = theme => ({
  root: {
    marginTop: theme.spacing.unit * 3,
  },
  progressRoot: {
    width: '100%',
  },
  snackbar: {
    margin: theme.spacing.unit,
  },
});

export default styles;
